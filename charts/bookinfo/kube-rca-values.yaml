gateway:
  enabled: false

ingress:
  enabled: true
  ingressClassName: nginx
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - kuberca-demo2.kkamji.net
  paths:
    - /
  tls:
    - secretName: kuberca-demo2-tls
      hosts:
        - kuberca-demo2.kkamji.net

# Resource limits based on observed usage (kubectl top po)
# load-generator is excluded.
resources:
  details:
    limits:
      cpu: 200m
      memory: 256Mi
  ratings:
    limits:
      cpu: 100m
      memory: 256Mi
  reviews:
    limits:
      cpu: 200m
      memory: 512Mi
  productpage:
    limits:
      cpu: 1000m
      memory: 1Gi
